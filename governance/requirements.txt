-r ../requirements.txt
evidently==0.5.1
pandas==2.2.3
scikit-learn==1.5.2
# P2-4 uses these directly (coding_bootstrap), not just transitively via
# scikit-learn, so they are pinned here rather than inherited silently.
numpy==2.0.2
scipy==1.15.3

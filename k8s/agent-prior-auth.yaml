apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-prior-auth
  namespace: care-ops
spec:
  replicas: 1
  selector: {matchLabels: {app: agent-prior-auth}}
  template:
    metadata: {labels: {app: agent-prior-auth}}
    spec:
      containers:
        - name: agent-prior-auth
          image: care-ops-agent_prior_auth:latest
          imagePullPolicy: IfNotPresent
          ports: [{containerPort: 8000}]
          envFrom:
            - secretRef: {name: care-ops-secrets}
          env:
            - {name: POSTGRES_HOST, value: db}
          readinessProbe:
            httpGet: {path: /health, port: 8000}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /health, port: 8000}
            initialDelaySeconds: 10
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: agent-prior-auth
  namespace: care-ops
spec:
  selector: {app: agent-prior-auth}
  ports: [{port: 8000, targetPort: 8000}]
